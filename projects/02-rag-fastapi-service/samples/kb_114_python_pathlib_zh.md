# Python 进阶：pathlib 路径

适用场景：跨平台拼接文件路径、遍历目录和检查文件是否存在时，`pathlib` 比手写字符串更稳妥。
核心方法：使用 `Path`、`/` 运算符、`glob()` 和 `read_text()`；路径来源于用户时先限制在允许目录内。
注意事项：相对路径依赖当前工作目录，部署和测试时应明确项目根目录，不要假设 Windows 和 Linux 分隔符相同。
