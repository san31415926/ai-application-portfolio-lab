# REST API 设计基础

## 资源思维
REST API 通常把业务对象设计成资源，并使用清晰的 URL 表示资源。例如 `/users` 表示用户集合，`/users/123` 表示编号为 123 的用户。

## 设计建议
使用 HTTP 方法表达动作，返回稳定的 JSON 结构，统一错误格式，并为分页、筛选和排序保留明确的参数。不要把所有操作都设计成没有语义的 `/doSomething`。

## 实践检查
接口文档应说明请求参数、响应字段、状态码和示例。前后端联调时，先用 Swagger 或 curl 验证接口契约，再接入页面，能减少重复排查。
