# JSON 数据格式

## 基本结构
JSON 由对象、数组、字符串、数字、布尔值和 `null` 组成。对象使用键值对，数组使用有序列表。键名必须使用双引号，不能写注释。

## Python 示例
Python 中可以使用 `json.loads` 把字符串解析成对象，使用 `json.dumps` 把对象序列化成字符串。读取外部 JSON 时，要考虑格式错误、字段缺失和类型不符合预期的情况。

## 接口注意事项
API 返回 JSON 时，字段命名和类型要稳定。日期、金额和枚举值应约定统一格式，避免同一个字段在不同接口中一会儿是字符串、一会儿是数字。
