# HTML 页面结构

## 语义标签
HTML 用标签描述页面结构。`header`、`nav`、`main`、`section`、`article` 和 `footer` 能让浏览器、搜索引擎和辅助技术更好理解页面。

## 内容组织
页面应有清晰的标题层级，通常一个页面只保留一个主要 `h1`。链接用于跳转，按钮用于触发操作，不能只为了样式而混用两者。

## 实践建议
图片要写有意义的 `alt` 文本，表单控件要关联 `label`，外部链接和按钮文字要能独立表达用途。良好的 HTML 结构会降低 CSS 和 JavaScript 的复杂度。
