# CSS 布局基础

## 盒模型
元素由内容、内边距、边框和外边距组成。使用 `box-sizing: border-box` 后，设置的宽度会包含内边距和边框，更容易控制布局尺寸。

## Flex 与 Grid
Flex 适合一维排列，例如导航栏和工具条；Grid 适合二维布局，例如卡片列表和后台页面。布局时应先确定容器关系，再调整间距和对齐方式。

## 响应式原则
使用相对单位、弹性宽度和媒体查询适配不同屏幕。固定格式的按钮、表格和输入框要设置稳定的最小尺寸，避免文字变化导致页面跳动。
