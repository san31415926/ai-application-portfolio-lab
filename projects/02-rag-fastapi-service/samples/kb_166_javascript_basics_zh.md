# JavaScript 基础

## 变量与函数
使用 `const` 表示不会重新赋值的引用，使用 `let` 表示可能变化的变量。函数用于封装可复用逻辑，应让参数、返回值和副作用尽量清晰。

## 异步操作
网络请求通常是异步的，可以使用 `async` 和 `await` 让代码更易读。异步函数要处理加载、成功和失败三种状态，不能只在成功时更新页面。

## 排查问题
先在浏览器控制台确认错误位置，再检查变量值和网络响应。不要用大量 `alert` 代替调试，也不要忽略 Promise 的异常。
