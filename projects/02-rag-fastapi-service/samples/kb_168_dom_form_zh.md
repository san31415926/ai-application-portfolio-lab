# DOM 与表单交互

## DOM 操作
JavaScript 可以通过 `querySelector` 找到元素，通过 `textContent` 更新文本，通过 `addEventListener` 监听用户操作。更新用户输入时应优先使用安全的文本属性，避免把不可信内容直接插入 HTML。

## 表单流程
提交表单时先阻止默认刷新，读取输入值，做前端提示，再向 API 发送请求。服务端仍必须重新校验，因为前端校验不能代替安全边界。

## 用户体验
提交期间按钮应显示忙碌状态，成功后给出结果，失败时保留用户输入并展示可理解的错误信息。空状态和网络断开状态也应有明确反馈。
