# 响应式与无障碍基础

## 响应式页面
页面应能在手机、平板和桌面屏幕上使用。优先采用流式布局和可换行内容，避免让固定宽度容器在小屏幕上产生横向滚动。

## 键盘操作
可交互元素必须能通过键盘访问，并有清晰的焦点状态。按钮、链接、输入框应使用原生语义，不能只依赖鼠标悬停。

## 对比度与反馈
文字和背景需要足够对比度，错误提示不能只用颜色区分。加载、成功、失败和禁用状态要同时有文本或图标反馈。
