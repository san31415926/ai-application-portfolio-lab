# TypeScript 入门

## 类型作用
TypeScript 在 JavaScript 基础上增加静态类型检查，可以提前发现字段拼写错误、参数类型错误和可能为空的值。类型检查不能保证运行时数据一定可信。

## 常见写法
可以为函数参数和返回值声明类型，为接口响应定义 `interface` 或 `type`。联合类型适合表示有限的状态，例如 `loading | success | error`。

## 实践建议
先给接口边界、表单数据和核心函数加类型，再逐步覆盖旧代码。不要为了消除报错而到处使用 `any`，应尽量说明真实的数据结构。
