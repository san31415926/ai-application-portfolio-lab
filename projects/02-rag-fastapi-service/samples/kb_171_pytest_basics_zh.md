# pytest 测试入门

## 测试结构
pytest 会自动发现以 `test_` 开头的文件、函数和方法。一个测试应准备输入、执行目标代码，并断言可观察的结果。

## 常用能力
`assert` 用于断言，fixture 用于准备可复用的测试环境，参数化测试适合用多组输入验证同一条规则。测试名称应表达行为，而不是实现细节。

## 测试重点
先覆盖正常输入、空输入、边界值和错误输入。测试失败时先读第一处断言和堆栈，再确认是代码问题、环境问题还是测试预期过时。
