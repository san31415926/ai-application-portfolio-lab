# API 接口测试

## 测试内容
接口测试要验证路径、方法、请求参数、状态码、响应结构和业务结果。除了成功请求，还要覆盖参数缺失、类型错误、资源不存在和未授权等情况。

## 测试隔离
每个测试应拥有可控的初始状态，避免依赖上一个测试留下的数据。内存服务可以在 `setUp` 中清空，数据库测试则应使用事务或独立测试库。

## 价值
API 测试能保护前后端之间的契约。对 RAG 服务来说，还应验证文档导入、索引统计、检索来源和无证据拒答，避免模型回答脱离资料。
