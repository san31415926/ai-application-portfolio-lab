# FastAPI 服务启动与部署

## 本地启动
可以使用 `python -m uvicorn app.main:app --reload` 启动开发服务。`--reload` 只适合开发阶段，会监控文件变化并自动重启。

## 配置检查
部署前要确认监听地址、端口、环境变量、上传目录和模型服务地址。生产环境通常不应把调试信息和内部异常直接返回给用户。

## 运行验证
启动后先访问健康检查接口，再访问 OpenAPI 文档，最后执行一次真实的导入和查询。这样能分别确认进程、路由和业务链路都正常。
