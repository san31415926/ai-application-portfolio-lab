# 日志与运行监控

## 有用日志
日志应记录请求方法、路径、耗时、状态码、错误类型和必要的业务标识。不要记录密码、完整 Token、身份证号或未经处理的隐私内容。

## 日志级别
`DEBUG` 用于开发排查，`INFO` 记录正常流程，`WARNING` 记录可恢复异常，`ERROR` 记录需要处理的失败。生产环境应通过配置控制日志级别。

## RAG 监控指标
可以关注文档数、分块数、检索耗时、最高相似度、拒答比例和模型响应耗时。指标能帮助判断问题出在数据、检索还是生成环节。
