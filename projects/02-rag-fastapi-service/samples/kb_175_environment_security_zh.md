# 环境变量与密钥安全

## 配置分离
端口、模型名称、数据库地址和日志级别适合通过环境变量配置。这样同一份代码可以在本地、测试和部署环境使用不同设置。

## 密钥保护
API Key、数据库密码和 Token 不应写进源代码、README 或提交记录。应使用本地 `.env`、系统密钥管理服务，并把 `.env` 加入 `.gitignore`。

## 排查泄露
如果密钥已经提交到 Git，不能只删除当前文件，还应立即轮换密钥，并检查历史提交、日志和构建产物。公开项目尤其要在推送前检查敏感信息。
