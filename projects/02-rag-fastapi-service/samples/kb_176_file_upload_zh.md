# 文件上传接口

## 基本流程
客户端选择文件后，以 `multipart/form-data` 发送请求。服务端应检查文件名、扩展名、大小和实际内容，再决定是否保存和解析。

## 安全边界
不能只相信客户端传来的 MIME 类型或文件后缀。保存时应生成服务端文件名，限制上传目录，避免路径穿越，并防止覆盖已有文件。

## 文档入库
RAG 应用在上传后完成读取、清洗、切片、索引和统计。解析失败要返回明确错误，不能把半成功状态伪装成已入库。
