# 接口限流基础

## 为什么限流
限流可以避免单个用户耗尽 CPU、内存、模型额度或数据库连接，也能降低恶意请求带来的风险。生成式 AI 接口尤其需要限制并发和请求频率。

## 常见策略
固定窗口容易在窗口边界出现突发流量，滑动窗口更平滑，令牌桶适合允许短时突发。策略应按用户、IP、接口和模型成本分别考虑。

## 返回方式
超过限制时应返回 `429 Too Many Requests`，并可以通过 `Retry-After` 告知重试时间。前端应退避重试，不能立即无限循环请求。
