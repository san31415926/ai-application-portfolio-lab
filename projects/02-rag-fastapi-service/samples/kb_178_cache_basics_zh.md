# 缓存使用基础

## 适合缓存的内容
不经常变化、读取成本较高的内容适合缓存，例如模型列表、文档统计和重复的检索结果。带有用户权限或实时变化的数据不能简单共用缓存。

## 失效策略
缓存必须考虑过期时间、主动删除和版本号。更新原始数据后如果缓存没有失效，用户可能持续看到旧结果。

## RAG 场景
可以缓存文档 Embedding、热门问题的检索结果和模型发现结果。缓存键应包含查询文本、模型、知识库版本和关键参数，避免不同请求相互污染。
