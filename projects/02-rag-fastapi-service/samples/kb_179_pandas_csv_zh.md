# Pandas 读取 CSV

## 读取数据
Pandas 的 `read_csv` 可以把 CSV 读取为 `DataFrame`。读取时要关注编码、分隔符、表头、日期列和缺失值，否则后续统计结果可能不可靠。

## 基本检查
可以使用 `head`、`info`、`describe` 和 `value_counts` 快速了解数据。先确认行列数量、字段类型和重复记录，再开始分析。

## 导出结果
处理完成后可使用 `to_csv` 导出。导出给中文用户时，常需要指定合适的编码，并确认 Excel 或其他工具能够正常打开。
