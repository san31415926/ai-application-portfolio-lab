# 正则表达式入门

## 常见符号
`.` 匹配任意单个字符，`*` 表示前一个模式重复零次或多次，`+` 表示至少一次，`^` 和 `$` 表示文本开始与结束。括号可用于分组和捕获。

## 使用场景
正则适合做格式初筛，例如提取邮箱、分割连续空白和查找日志编号。它不适合替代完整的 HTML、JSON 或编程语言解析器。

## 安全与测试
复杂正则可能出现灾难性回溯。使用前应准备正常、空值、超长和恶意输入，控制表达式复杂度，并为失败情况设置边界。
