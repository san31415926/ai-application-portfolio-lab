# `.gitignore` 使用方法

## 忽略内容
`.gitignore` 用规则告诉 Git 哪些文件不应被跟踪，常见内容包括虚拟环境、缓存、日志、构建产物、IDE 配置和本地密钥文件。

## 常见误区
已经被 Git 跟踪的文件不会因为后来加入 `.gitignore` 就自动消失，需要先从索引中移除再提交。规则要放在正确目录，并注意通配符范围。

## 项目建议
Python 项目通常忽略 `.venv/`、`__pycache__/`、`.pytest_cache/` 和 `.env`。提交前可以用 `git status` 检查是否有不该公开的文件。
