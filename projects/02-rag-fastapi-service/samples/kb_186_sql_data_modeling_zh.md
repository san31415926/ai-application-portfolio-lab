# SQL 数据建模基础

## 表与关系
数据建模要先识别实体、属性和实体之间的关系，再设计表、主键和外键。用户、订单和商品通常是不同实体，不能把所有信息塞进一张无限增长的表。

## 约束设计
主键保证记录唯一，外键维护关联关系，非空和唯一约束能减少脏数据。约束应和业务规则一致，不能只依赖应用层检查。

## 查询考虑
建模时既要考虑数据一致性，也要考虑常用查询。读多写少的场景可以通过索引或汇总表优化，但应先用真实查询测量，再做结构调整。
