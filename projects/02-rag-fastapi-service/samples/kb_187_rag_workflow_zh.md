# RAG 工作流程

## 六个环节
一个常见 RAG 流程包括文档加载、文本清洗、分块、Embedding、向量索引和检索生成。查询时先把问题转成检索条件，再把相关片段交给聊天模型。

## 为什么需要 RAG
模型参数不等于用户的私有知识库。RAG 可以在回答前提供外部资料，并返回来源，使回答更容易核验和更新。

## 判断效果
不能只看模型回答是否流畅，还要检查是否找对文档、来源是否相关、答案是否覆盖问题，以及没有证据时是否拒答。
