# 文档切片策略

## 切片目的
切片是把长文档拆成适合检索和上下文拼接的小段。太长会带入无关内容，太短会丢失上下文，合适大小要结合文档结构和模型上下文限制。

## 实践方法
优先按标题、段落和列表边界切分，必要时设置重叠区保留上下文。代码应尽量避免把一个函数、表格行或操作步骤从中间截断。

## 验证方式
对每个片段保留文件名、标题、顺序和来源位置。抽查问题对应的片段是否完整，是判断切片策略是否合理的直接方法。
