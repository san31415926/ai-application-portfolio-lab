# Embedding 向量表示

## 基本原理
Embedding 模型把文本转换成向量，使语义相近的文本在向量空间中更接近。它不是把文本翻译成另一种语言，而是生成用于比较和检索的数值表示。

## 使用流程
建库时为文档片段生成向量，查询时为用户问题生成向量，再计算相似度。文档和查询必须使用兼容的 Embedding 模型与相同的向量维度。

## 质量因素
文本清洗、切片大小、语言覆盖、模型选择和相似度算法都会影响结果。更大的向量模型不一定在所有中文教程场景都更好，应使用真实问题集评估。
