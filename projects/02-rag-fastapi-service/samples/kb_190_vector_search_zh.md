# 向量检索基础

## 检索过程
向量检索先计算查询向量与文档向量的相似度，再返回 Top-K 片段。余弦相似度常用来比较方向，距离越近通常表示语义越相似。

## 混合检索
关键词检索擅长精确匹配文件名、命令和专有名词，向量检索擅长理解近义表达。实际项目可以把两者结合，再根据分数或排序策略得到结果。

## 结果处理
检索结果要保留分数和来源信息，并设置最低相关度阈值。Top-K 不是越大越好，加入过多低质量片段会增加噪声和上下文成本。
