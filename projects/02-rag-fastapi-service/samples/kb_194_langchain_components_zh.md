# LangChain 组件理解

## 常见组件
LangChain 可以把文档加载器、文本切分器、Embedding、向量存储、Retriever 和 LLM 组合起来。它解决的是应用编排问题，不会自动保证数据和答案正确。

## 链路思维
可以把每一步拆开调试：先确认文档读取，再确认切片，再确认向量生成和检索，最后确认提示词与模型输出。出现问题时不要只盯着最后的回答。

## 选择建议
小型项目可以先使用简单的 Python 服务和清晰接口，等链路稳定后再引入更复杂的 Chain 或 Agent。抽象层越多，越要保留日志和可替换的组件边界。
