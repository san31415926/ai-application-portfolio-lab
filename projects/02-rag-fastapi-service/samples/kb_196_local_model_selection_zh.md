# 本地模型选择方法

## 先看任务
简单抽取和分类可以选择小模型，中文问答和总结需要更好的语言能力，代码解释还要看模型的编程训练质量。模型参数量只是参考，不是唯一标准。

## 评估维度
比较中文表达、遵循提示词、引用上下文、拒答能力、响应速度和内存占用。使用同一组问题和同一套知识库测试，结果才有可比性。

## 页面切换模型
如果应用支持选择模型，前端应从服务端获取已安装模型列表，提交查询时传入选择结果。服务端必须再次校验模型是否存在，不能相信前端传来的任意名称。
