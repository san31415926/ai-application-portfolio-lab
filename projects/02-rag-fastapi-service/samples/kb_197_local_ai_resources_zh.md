# 本地 AI 资源规划

## 资源组成
运行本地模型需要考虑系统内存、显存、磁盘空间、模型量化方式和并发请求数。Embedding 模型与聊天模型可以分开选择，不必都使用同一规模。

## 实际策略
开发时可以使用小模型快速验证接口，演示时再切换到质量更好的模型。知识库索引可以提前完成，避免每次启动都重复计算 Embedding。

## 故障处理
模型服务停止、模型未安装或响应超时时，API 应返回可理解的错误，并保留提取式回答或拒答路径。不能把本地模型的异常堆栈直接展示给最终用户。
