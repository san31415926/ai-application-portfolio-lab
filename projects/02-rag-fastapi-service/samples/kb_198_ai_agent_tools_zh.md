# AI Agent 与工具调用

## 基本概念
Agent 通常根据任务决定下一步动作，并调用搜索、数据库、计算器或业务 API。工具应有明确的输入输出契约，不能让模型直接执行任意系统命令。

## 风险控制
高风险操作要经过权限校验、参数校验和人工确认。工具调用应记录调用者、参数摘要、结果和耗时，并限制循环次数，防止任务无限执行。

## 何时使用
如果问题只需要从固定文档检索，普通 RAG 更容易理解和测试；只有当任务需要多步规划或访问多个工具时，才值得引入 Agent。
